---
phase: 01-robustheit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/provider.js
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "Ein fehlgeschlagener Storytel-API-Call wirft einen unterscheidbaren Fehler (kein stilles { matches: [] })"
    - "Der Server kann 'no results' von 'API failed' unterscheiden"
    - "Ein DB-Fehler beim Cache-Lesen bricht die Suche nicht ab — Fallback auf direkten API-Call"
    - "Ein DB-Fehler beim Server-Start (korrupte DB) führt zu einem sauberen Fehlerlog, nicht zu einem unhandled crash"
  artifacts:
    - path: "src/provider.js"
      provides: "Fehler-differenzierung und DB-Fallback"
      contains: "class StorytelApiError"
  key_links:
    - from: "src/provider.js searchBooks()"
      to: "src/server.js catch-Block"
      via: "throws StorytelApiError statt returning { matches: [] }"
      pattern: "throw.*StorytelApiError"
---

<objective>
Fehler in provider.js sichtbar und unterscheidbar machen: API-Fehler werden geworfen (nicht geschluckt), DB-Fehler werden abgefangen mit Fallback auf direkten API-Call.

Purpose: ERR-01 (unterscheidbare Fehlertypen), ERR-02 (no results vs. API failed), ERR-03 (DB-Fallback)
Output: provider.js mit StorytelApiError-Klasse, DB-Fehlerbehandlung, und klar getrennten Fehlerpfaden
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aktueller Fehlerpfad in provider.js (wird geändert): -->
<!-- searchBooks() catch-Block, Zeile 458-461: -->
```javascript
} catch (error) {
    console.error('Error searching books:', error.message);
    return { matches: [] };  // ← stilles Versagen, nicht unterscheidbar von "keine Ergebnisse"
}
```

<!-- Aktueller Cache-Lese-Code ohne Fehlerbehandlung, Zeilen 412-415: -->
```javascript
const cached = getCache.get(cacheKey);
if (cached) {
    return JSON.parse(cached.response);  // ← kein try/catch bei DB-Fehler
}
```

<!-- DB-Initialisierung ohne Fehlerbehandlung, Zeilen 12-26: -->
```javascript
const db = new Database(dbPath);
db.pragma('journal_mode = WAL');
db.exec(`CREATE TABLE IF NOT EXISTS ...`);
```

<!-- Was server.js aktuell erwartet (darf sich ändern): -->
<!-- provider.searchBooks() gibt { matches: [] } zurück — server.js macht res.json(results) -->
<!-- Nach Änderung: server.js muss StorytelApiError fangen und 503 zurückgeben -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StorytelApiError-Klasse einführen und searchBooks() Fehler werfen lassen</name>
  <files>src/provider.js</files>
  <read_first>
    - src/provider.js (vollständig — aktuellen Stand verstehen, besonders Zeilen 403-462)
  </read_first>
  <action>
Am Anfang von provider.js (vor der Database-Initialisierung) eine `StorytelApiError`-Klasse definieren:

```javascript
class StorytelApiError extends Error {
    constructor(message, cause) {
        super(message);
        this.name = 'StorytelApiError';
        this.cause = cause;
    }
}
```

Dann in `searchBooks()` den catch-Block (Zeilen 458-461) von "stilles Versagen" in "Fehler werfen" ändern:

```javascript
} catch (error) {
    console.error('Error searching books:', error.message);
    throw new StorytelApiError('Storytel API request failed', error);
}
```

`StorytelApiError` am Ende des Moduls exportieren:
```javascript
module.exports = { StorytelProvider, StorytelApiError };
```

WICHTIG: `src/server.js` importiert aktuell `require('./provider')` als direkte Klasse. Nach diesem Export-Umbau auf `const { StorytelProvider, StorytelApiError } = require('./provider');` ändern — aber das passiert in Plan 02. In Plan 02 werden die catch-Blöcke in server.js angepasst, die den Fehler als 503 zurückgeben.

Für diesen Task reicht es, den Export zu ändern und zu dokumentieren dass server.js in Plan 02 angepasst wird.
  </action>
  <verify>
    <automated>node -e "const { StorytelProvider, StorytelApiError } = require('./src/provider'); const e = new StorytelApiError('test', null); console.log(e.name === 'StorytelApiError' && e instanceof Error ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class StorytelApiError" src/provider.js` findet die Klasse
    - `grep -n "throw new StorytelApiError" src/provider.js` findet den throw im catch-Block
    - `grep -n "module.exports" src/provider.js` zeigt `{ StorytelProvider, StorytelApiError }`
    - `node -e "const { StorytelProvider, StorytelApiError } = require('./src/provider'); console.log('ok')"` gibt keine Fehler
  </acceptance_criteria>
  <done>searchBooks() wirft StorytelApiError bei API-Fehler statt { matches: [] } zurückzugeben. Klasse ist exportiert.</done>
</task>

<task type="auto">
  <name>Task 2: DB-Initialisierung und Cache-Zugriffe absichern (ERR-03)</name>
  <files>src/provider.js</files>
  <read_first>
    - src/provider.js (aktueller Stand nach Task 1 — besonders DB-Initialisierung Zeilen 6-26 und searchBooks Zeilen 403-462)
  </read_first>
  <action>
Die DB-Initialisierung (Zeilen 6-26) in einen try/catch-Block einwickeln. Bei Fehler: Variable `db` auf `null` setzen und Fehler loggen, aber NICHT crashen:

```javascript
let db = null;
let getCache = null;
let setCache = null;

try {
    db = new Database(dbPath);
    db.pragma('journal_mode = WAL');
    db.exec(`
        CREATE TABLE IF NOT EXISTS search_cache (
            cache_key TEXT PRIMARY KEY,
            response TEXT NOT NULL,
            created_at INTEGER NOT NULL
        )
    `);
    db.exec(`DELETE FROM search_cache WHERE response = '{"matches":[]}'`);
    getCache = db.prepare('SELECT response FROM search_cache WHERE cache_key = ?');
    setCache = db.prepare('INSERT OR REPLACE INTO search_cache (cache_key, response, created_at) VALUES (?, ?, ?)');
    console.log(`[cache] SQLite cache initialized at ${dbPath}`);
} catch (err) {
    console.error(`[cache] Failed to initialize SQLite cache at ${dbPath}: ${err.message}. Running without cache.`);
    db = null;
}
```

In `searchBooks()` den Cache-Lese-Block anpassen — nur ausführen wenn `getCache` nicht null ist, und in try/catch:

```javascript
if (getCache) {
    try {
        const cached = getCache.get(cacheKey);
        if (cached) {
            console.log(`[cache] HIT for "${cacheKey}"`);
            return JSON.parse(cached.response);
        }
    } catch (err) {
        console.error(`[cache] Read error for "${cacheKey}": ${err.message}. Falling back to API.`);
    }
}
```

Den Cache-Write-Block analog absichern:

```javascript
if (matches.length > 0 && setCache) {
    try {
        setCache.run(cacheKey, JSON.stringify(result), Date.now());
        console.log(`[cache] WRITE for "${cacheKey}"`);
    } catch (err) {
        console.error(`[cache] Write error for "${cacheKey}": ${err.message}`);
    }
}
```

WICHTIG: Die API-Suche selbst läuft immer — auch wenn der Cache fehlschlägt. Der Fallback ist implizit: Cache-Fehler wird geloggt, dann läuft der Code normal weiter zum API-Call.
  </action>
  <verify>
    <automated>node -e "
process.env.CACHE_DB = '/tmp/test_nonexistent_dir_xyz/cache.db';
// Dieser Pfad existiert nicht und kann nicht erstellt werden wenn das dir fehlt
// aber mkdirSync recursive sollte es anlegen. Besser: beschädigtes DB-File testen
const { StorytelProvider } = require('./src/provider');
console.log('Module loaded without crash despite potential DB issues: OK');
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "let db = null" src/provider.js` findet die nullable DB-Variable
    - `grep -n "Running without cache" src/provider.js` findet den Fallback-Log
    - `grep -n "if (getCache)" src/provider.js` findet die null-Prüfung vor dem Cache-Read
    - `grep -n "if (matches.length > 0 && setCache)" src/provider.js` findet die null-Prüfung vor dem Cache-Write
    - `grep -n "Falling back to API" src/provider.js` findet den Fallback-Hinweis bei Cache-Read-Fehler
    - `node -e "const { StorytelProvider } = require('./src/provider'); console.log('ok')"` gibt keine Fehler
  </acceptance_criteria>
  <done>DB-Fehler bei Initialisierung und beim Cache-Zugriff crashen den Service nicht mehr. Fallback auf direkten API-Call ist aktiv.</done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
1. `node -e "const { StorytelProvider, StorytelApiError } = require('./src/provider'); console.log('imports ok')"` — kein Fehler
2. `grep -n "class StorytelApiError" src/provider.js` — Klasse gefunden
3. `grep -n "throw new StorytelApiError" src/provider.js` — throw gefunden
4. `grep -n "let db = null" src/provider.js` — nullable DB gefunden
5. `grep -n "if (getCache)" src/provider.js` — null-guard gefunden
</verification>

<success_criteria>
- provider.js wirft StorytelApiError bei API-Fehler (ERR-01, ERR-02)
- DB-Fehler werden abgefangen, Service läuft ohne Cache weiter (ERR-03)
- Modul lädt ohne Crash, auch wenn DB beschädigt oder nicht erreichbar ist
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-robustheit/01-01-SUMMARY.md` erstellen.
</output>
