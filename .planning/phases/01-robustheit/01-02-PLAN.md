---
phase: 01-robustheit
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/server.js
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - ERR-01
  - ERR-02

must_haves:
  truths:
    - "CORS-Ursprünge sind über ALLOWED_ORIGINS env var einschränkbar — kein Wildcard * im Default"
    - "Auth-Token-Vergleich ist timing-safe (crypto.timingSafeEqual)"
    - "Eine Suchanfrage mit mehr als 200 Zeichen wird mit HTTP 400 abgewiesen"
    - "Eine Suchanfrage mit unerlaubten Sonderzeichen (<, >, {, }, |, \\) wird mit HTTP 400 abgewiesen"
    - "Eine fehlgeschlagene Storytel-API-Anfrage liefert HTTP 503 mit error-Feld, nicht leeres matches-Array"
  artifacts:
    - path: "src/server.js"
      provides: "CORS-Config, timing-safe Auth, Query-Validierung, StorytelApiError-Handling"
      contains: "crypto.timingSafeEqual"
  key_links:
    - from: "src/server.js validateQuery middleware"
      to: "alle drei Search-Endpoints"
      via: "Middleware vor checkAuth und validateRegion"
      pattern: "validateQuery"
    - from: "src/server.js catch-Block"
      to: "StorytelApiError"
      via: "instanceof check → 503 statt 500"
      pattern: "instanceof StorytelApiError"
---

<objective>
server.js sicherheitshärten: CORS konfigurierbar machen, Auth timing-safe machen, Query-Validierung einführen, und die neuen StorytelApiError-Fehler aus Plan 01 korrekt als 503 zurückgeben.

Purpose: SEC-01 (CORS), SEC-02 (timing-safe Auth), SEC-03 (Query-Validierung), ERR-01/ERR-02 (Fehler-Differenzierung im Server)
Output: server.js mit hartetem Sicherheitsmodell und korrekter Fehler-Weiterleitung
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-robustheit/01-01-SUMMARY.md

<interfaces>
<!-- Nach Plan 01 exportiert provider.js: -->
```javascript
const { StorytelProvider, StorytelApiError } = require('./provider');
```

<!-- Aktueller server.js Import (muss angepasst werden): -->
```javascript
const StorytelProvider = require('./provider');  // ← veraltet nach Plan 01
```

<!-- Aktueller CORS (Zeile 10): -->
```javascript
app.use(cors());  // ← Wildcard * — alle Ursprünge erlaubt
```

<!-- Aktueller Auth-Vergleich (Zeile 15): -->
```javascript
req.headers.authorization !== auth  // ← direkter String-Vergleich, timing-angreifbar
```

<!-- Aktueller catch-Block (alle 3 Endpoints, z.B. Zeile 51-54): -->
```javascript
} catch (error) {
    console.error('Search error:', error);
    res.status(500).json({ error: 'Internal server error' });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CORS konfigurierbar und Auth timing-safe machen</name>
  <files>src/server.js</files>
  <read_first>
    - src/server.js (vollständig — aktuellen Stand verstehen)
    - src/provider.js (Export-Struktur nach Plan 01 prüfen)
  </read_first>
  <action>
1. Import-Zeile oben anpassen (provider.js exportiert jetzt named exports):
```javascript
const crypto = require('crypto');
const { StorytelProvider, StorytelApiError } = require('./provider');
```

2. CORS-Konfiguration ersetzen (Zeile 10 `app.use(cors())`):
```javascript
const allowedOrigins = process.env.ALLOWED_ORIGINS
    ? process.env.ALLOWED_ORIGINS.split(',').map(o => o.trim())
    : null;  // null = kein Wildcard-Default mehr, nur explizit erlaubte Origins

app.use(cors({
    origin: (origin, callback) => {
        // Erlaubt: kein Origin-Header (same-origin, curl, ABS-intern)
        if (!origin) return callback(null, true);
        // Erlaubt: wenn ALLOWED_ORIGINS gesetzt und Origin enthalten
        if (allowedOrigins && allowedOrigins.includes(origin)) return callback(null, true);
        // Abgelehnt: wenn ALLOWED_ORIGINS gesetzt aber Origin nicht enthalten
        if (allowedOrigins) return callback(new Error(`Origin ${origin} not allowed`), false);
        // Kein ALLOWED_ORIGINS gesetzt: alle Origins erlauben (Entwicklungsmodus)
        return callback(null, true);
    },
    credentials: true
}));
```

BEGRÜNDUNG für "alle erlauben wenn ALLOWED_ORIGINS nicht gesetzt": Das ist ein Single-User-Tool. Wenn der User ALLOWED_ORIGINS nicht konfiguriert, soll es wie bisher funktionieren. Der Security-Gewinn liegt darin, dass man es einschränken KANN.

3. Auth-Vergleich ersetzen (Zeile 14-18):
```javascript
const checkAuth = (req, res, next) => {
    if (!auth) return next();  // Kein Auth konfiguriert — durchlassen
    const provided = req.headers.authorization || '';
    // crypto.timingSafeEqual braucht Buffer gleicher Länge
    const authBuf = Buffer.from(auth);
    const providedBuf = Buffer.alloc(authBuf.length);
    Buffer.from(provided).copy(providedBuf);
    const valid = crypto.timingSafeEqual(authBuf, providedBuf)
        && provided.length === auth.length;  // Längen-Check separat (timingSafeEqual ignoriert Länge nicht, aber sicherheitshalber explizit)
    if (!valid) {
        return res.status(401).json({ error: 'Unauthorized' });
    }
    next();
};
```
  </action>
  <verify>
    <automated>node -e "
const crypto = require('crypto');
const { StorytelProvider, StorytelApiError } = require('./src/provider');
console.log('Imports OK');
// Prüfe dass timingSafeEqual im server.js vorkommt
const fs = require('fs');
const src = fs.readFileSync('./src/server.js', 'utf8');
if (!src.includes('timingSafeEqual')) throw new Error('timingSafeEqual nicht gefunden');
if (!src.includes('ALLOWED_ORIGINS')) throw new Error('ALLOWED_ORIGINS nicht gefunden');
console.log('Security checks OK');
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "timingSafeEqual" src/server.js` findet den Aufruf im checkAuth-Block
    - `grep -n "ALLOWED_ORIGINS" src/server.js` findet die env-var-Nutzung
    - `grep -n "require('crypto')" src/server.js` oder `grep -n 'require("crypto")' src/server.js` findet den Import
    - `grep -n "StorytelApiError" src/server.js` findet den Import
    - `node src/server.js &amp; sleep 1 &amp;&amp; kill %1` startet ohne Crash (oder `node -e "require('./src/server.js')"` schlägt nicht fehl — server.js braucht listen, nicht direkt prüfbar via node -e, aber Syntax-Check geht: `node --check src/server.js`)
    - `node --check src/server.js` gibt keine Syntaxfehler
  </acceptance_criteria>
  <done>CORS erlaubt Einschränkung via ALLOWED_ORIGINS. Auth nutzt crypto.timingSafeEqual. Provider-Import auf named exports umgestellt.</done>
</task>

<task type="auto">
  <name>Task 2: Query-Validierung und StorytelApiError als 503 behandeln</name>
  <files>src/server.js</files>
  <read_first>
    - src/server.js (aktueller Stand nach Task 1)
  </read_first>
  <action>
1. `validateQuery`-Middleware nach `validateRegion` einfügen:
```javascript
const validateQuery = (req, res, next) => {
    const query = req.query.query || req.query.title || '';
    if (query.length > 200) {
        return res.status(400).json({ error: 'Query too long (max 200 characters)' });
    }
    // Blockiere Sonderzeichen die in API-Queries nichts zu suchen haben
    // Erlaubt: Buchstaben, Zahlen, Leerzeichen, Bindestrich, Apostroph, Punkt, Komma, Doppelpunkt, Anführungszeichen
    if (/[<>{|}\\]/.test(query)) {
        return res.status(400).json({ error: 'Query contains invalid characters' });
    }
    next();
};
```

2. Die Middleware zu allen drei Endpoint-Definitionen hinzufügen — nach `checkAuth, validateRegion`:
```javascript
app.get('/:region/search', checkAuth, validateRegion, validateQuery, async (req, res) => {
app.get('/:region/book/search', checkAuth, validateRegion, validateQuery, async (req, res) => {
app.get('/:region/audiobook/search', checkAuth, validateRegion, validateQuery, async (req, res) => {
```

3. Alle drei catch-Blöcke so anpassen, dass StorytelApiError als 503 zurückgegeben wird:

Für `/:region/search` (Zeile ~51-54):
```javascript
} catch (error) {
    if (error instanceof StorytelApiError) {
        console.error('Storytel API error:', error.message);
        return res.status(503).json({ error: 'Storytel API unavailable', details: error.message });
    }
    console.error('Search error:', error);
    res.status(500).json({ error: 'Internal server error' });
}
```

Für `/:region/book/search` und `/:region/audiobook/search` identisch.

WICHTIG: Der audiobook-Endpoint-catch-Block befindet sich nach dem stats-Block (Zeile ~102-105). Dort auch den StorytelApiError-Check hinzufügen.
  </action>
  <verify>
    <automated>node --check src/server.js &amp;&amp; echo "Syntax OK" &amp;&amp; grep -c "validateQuery" src/server.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/server.js` gibt keine Fehler (Exit-Code 0)
    - `grep -n "validateQuery" src/server.js` findet: die Middleware-Definition UND alle drei Endpoint-Definitionen (mindestens 4 Treffer)
    - `grep -n "max 200 characters" src/server.js` findet die Fehlermeldung
    - `grep -n "invalid characters" src/server.js` findet die Fehlermeldung
    - `grep -c "instanceof StorytelApiError" src/server.js` gibt 3 (einmal pro Endpoint)
    - `grep -n "503" src/server.js` findet die 503-Antworten
  </acceptance_criteria>
  <done>Query-Validierung blockt Anfragen über 200 Zeichen und mit unerlaubten Sonderzeichen mit 400. Storytel-API-Fehler werden als 503 zurückgegeben, nicht als leere matches-Arrays.</done>
</task>

</tasks>

<verification>
Abschlusskontrolle aller Phase-1-Anforderungen:

1. ERR-01: `grep -n "StorytelApiError" src/provider.js src/server.js` — in beiden Dateien vorhanden
2. ERR-02: `grep -n "503" src/server.js` — 503-Status für API-Fehler vorhanden
3. ERR-03: `grep -n "Running without cache\|Falling back to API" src/provider.js` — Fallback-Logs vorhanden
4. SEC-01: `grep -n "ALLOWED_ORIGINS" src/server.js` — CORS-Konfiguration vorhanden
5. SEC-02: `grep -n "timingSafeEqual" src/server.js` — timing-sicherer Vergleich vorhanden
6. SEC-03: `grep -n "validateQuery\|max 200\|invalid characters" src/server.js` — Validierung vorhanden
7. Syntaxprüfung: `node --check src/server.js src/provider.js` — keine Fehler
</verification>

<success_criteria>
- CORS ist einschränkbar via ALLOWED_ORIGINS env var (SEC-01)
- Auth-Vergleich nutzt crypto.timingSafeEqual (SEC-02)
- Queries über 200 Zeichen oder mit <>{|}\\ werden mit 400 abgelehnt (SEC-03)
- Storytel-API-Fehler geben HTTP 503 zurück, nicht leere matches (ERR-01, ERR-02)
- `node --check src/server.js` gibt keine Syntaxfehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-robustheit/01-02-SUMMARY.md` erstellen.
</output>
