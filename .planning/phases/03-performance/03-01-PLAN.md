---
phase: 03-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.js
autonomous: true
requirements:
  - PERF-02

must_haves:
  truths:
    - "Storytel API requests timeout nach maximal 15 Sekunden"
    - "Der Timeout-Wert ist als Konstante konfigurierbar via AXIOS_TIMEOUT_MS env var"
    - "Ein Log-Eintrag mit dem konfigurierten Timeout erscheint beim Serverstart"
  artifacts:
    - path: "src/config.js"
      provides: "AXIOS_TIMEOUT_MS auf 15000 ms Default"
      contains: "15000"
  key_links:
    - from: "src/config.js"
      to: "src/provider.js"
      via: "require('./config').AXIOS_TIMEOUT_MS"
      pattern: "AXIOS_TIMEOUT_MS"
---

<objective>
Axios-Timeout von 30 Sekunden auf 15 Sekunden reduzieren.

Purpose: Langsame oder hängende Storytel-API-Anfragen blockieren aktuell bis zu 30 Sekunden. 15 Sekunden ist ausreichend für eine funktionierende API und reduziert Wartezeit bei Ausfällen.
Output: src/config.js mit geändertem Default, server.js loggt den konfigurierten Timeout beim Start.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config.js
@src/server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Axios-Timeout auf 15 Sekunden reduzieren und beim Start loggen</name>
  <files>src/config.js, src/server.js</files>
  <read_first>src/config.js, src/server.js</read_first>
  <action>
    In src/config.js: Ändere den Default-Wert für AXIOS_TIMEOUT_MS von '30000' auf '15000'.
    Vorher: `AXIOS_TIMEOUT_MS: parseInt(process.env.AXIOS_TIMEOUT_MS || '30000', 10)`
    Nachher: `AXIOS_TIMEOUT_MS: parseInt(process.env.AXIOS_TIMEOUT_MS || '15000', 10)`

    In src/server.js: Ergänze den bestehenden logger.info-Aufruf beim Serverstart um den konfigurierten Timeout.
    Importiere AXIOS_TIMEOUT_MS aus config.js (bereits importiert: `const { PORT, DEFAULT_LIMIT } = require('./config');` — ergänze AXIOS_TIMEOUT_MS).
    Ändere den Startup-Log von:
    `logger.info({ port }, 'Storytel provider listening');`
    zu:
    `logger.info({ port, axiosTimeoutMs: AXIOS_TIMEOUT_MS }, 'Storytel provider listening');`

    Kein weiterer Code nötig — provider.js liest AXIOS_TIMEOUT_MS bereits aus config.js.
  </action>
  <verify>
    node -e "const c = require('./src/config'); console.assert(c.AXIOS_TIMEOUT_MS === 15000, 'wrong timeout: ' + c.AXIOS_TIMEOUT_MS); console.log('OK: AXIOS_TIMEOUT_MS =', c.AXIOS_TIMEOUT_MS);"
  </verify>
  <acceptance_criteria>
    - `grep "15000" src/config.js` gibt einen Treffer zurück
    - `grep "30000" src/config.js` gibt keinen Treffer zurück
    - `grep "axiosTimeoutMs" src/server.js` gibt einen Treffer zurück
    - `node -e "require('./src/config')" src/config.js` läuft ohne Fehler
  </acceptance_criteria>
  <done>AXIOS_TIMEOUT_MS Default ist 15000, Serverstart-Log enthält axiosTimeoutMs-Feld</done>
</task>

</tasks>

<verification>
node -e "const c = require('./src/config'); console.assert(c.AXIOS_TIMEOUT_MS === 15000, 'FAIL: timeout is ' + c.AXIOS_TIMEOUT_MS); console.log('PASS: timeout =', c.AXIOS_TIMEOUT_MS);"
grep -n "axiosTimeoutMs" src/server.js
</verification>

<success_criteria>
- AXIOS_TIMEOUT_MS Default ist 15000 (war 30000)
- src/server.js loggt axiosTimeoutMs beim Start
- Kein alter 30000-Wert mehr in config.js
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-performance/03-01-SUMMARY.md` erstellen.
</output>
