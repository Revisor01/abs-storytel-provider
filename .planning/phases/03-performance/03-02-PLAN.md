---
phase: 03-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/provider.js
  - src/config.js
autonomous: true
requirements:
  - PERF-01
  - PERF-03

must_haves:
  truths:
    - "Beim Serverstart werden Cache-Einträge älter als 30 Tage gelöscht und VACUUM ausgeführt"
    - "Zwei parallele identische Suchanfragen lösen nur einen einzigen Storytel-API-Call aus"
    - "Das zweite parallele Request erhält dasselbe Ergebnis wie das erste ohne eigenen API-Call"
    - "Die Eviction-TTL ist als Konstante konfigurierbar"
  artifacts:
    - path: "src/provider.js"
      provides: "Cache-Eviction beim Start + in-flight Deduplication Map"
      contains: "inFlight"
    - path: "src/config.js"
      provides: "CACHE_EVICTION_DAYS Konstante"
      contains: "CACHE_EVICTION_DAYS"
  key_links:
    - from: "src/provider.js (DB-Init)"
      to: "search_cache"
      via: "DELETE WHERE created_at < threshold + VACUUM"
      pattern: "CACHE_EVICTION_DAYS"
    - from: "src/provider.js (searchBooks)"
      to: "inFlight Map"
      via: "Map.get(cacheKey) / Map.set(cacheKey, promise)"
      pattern: "inFlight"
---

<objective>
Cache-Eviction für SQLite (PERF-01) und Request-Deduplication für parallele identische Anfragen (PERF-03) in provider.js implementieren.

Purpose: Ohne Eviction wächst cache.db unbegrenzt. Ohne Deduplication lösen gleichzeitige identische Suchanfragen (z. B. vom ABS-Client bei schnellem Tippen) mehrere überflüssige API-Calls aus.
Output: provider.js mit Eviction beim Init und in-flight Map in searchBooks(); config.js mit CACHE_EVICTION_DAYS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/provider.js
@src/config.js
</context>

<interfaces>
<!-- Bestehende DB-Init-Struktur in provider.js (Zeilen 14-39) -->
<!-- Nach db.exec für Table-Creation und dem DELETE für leere Einträge kommt: -->
<!--   getCache = db.prepare(...) -->
<!--   setCache = db.prepare(...) -->
<!-- Eviction-Code muss NACH dem Table-CREATE und VOR den prepares eingefügt werden -->

<!-- searchBooks()-Signatur (Zeile 433): -->
<!--   async searchBooks(query, author = '', locale, type = 'all', limit = DEFAULT_LIMIT) -->
<!-- cacheKey wird auf Zeile 439 gebildet: `${formattedQuery}-${locale}-${type}` -->
<!-- In-flight Map muss NACH dem Cache-Check aber VOR dem fetchFromApi-Aufruf greifen -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CACHE_EVICTION_DAYS Konstante in config.js ergänzen</name>
  <files>src/config.js</files>
  <read_first>src/config.js</read_first>
  <action>
    Füge eine neue Konstante in src/config.js hinzu:
    `CACHE_EVICTION_DAYS: parseInt(process.env.CACHE_EVICTION_DAYS || '30', 10),`

    Die Datei hat aktuell 9 Zeilen. Füge die Konstante als letzten Eintrag vor dem abschließenden `}` ein.

    Resultierende Konstanten in config.js:
    - PORT
    - AXIOS_TIMEOUT_MS (Default durch Plan 01 auf 15000 geändert — falls Plan 01 noch nicht ausgeführt, bleibt er bei 30000, das ist für diesen Plan irrelevant)
    - DEFAULT_LIMIT
    - MAX_LIMIT
    - CACHE_DB_PATH
    - LOG_LEVEL
    - CACHE_EVICTION_DAYS (neu)
  </action>
  <verify>
    node -e "const c = require('./src/config'); console.assert(c.CACHE_EVICTION_DAYS === 30, 'wrong: ' + c.CACHE_EVICTION_DAYS); console.log('OK: CACHE_EVICTION_DAYS =', c.CACHE_EVICTION_DAYS);"
  </verify>
  <acceptance_criteria>
    - `grep "CACHE_EVICTION_DAYS" src/config.js` gibt einen Treffer zurück
    - `node -e "require('./src/config').CACHE_EVICTION_DAYS"` gibt 30 zurück
  </acceptance_criteria>
  <done>CACHE_EVICTION_DAYS ist als Konstante mit Default 30 in config.js verfügbar</done>
</task>

<task type="auto">
  <name>Task 2: Cache-Eviction beim DB-Init und Request-Deduplication in searchBooks</name>
  <files>src/provider.js</files>
  <read_first>src/provider.js</read_first>
  <action>
    TEIL A — Cache-Eviction beim DB-Init:

    In src/provider.js den Import-Block oben erweitern:
    Ändere Zeile 6:
    `const { AXIOS_TIMEOUT_MS, DEFAULT_LIMIT, MAX_LIMIT, CACHE_DB_PATH } = require('./config');`
    zu:
    `const { AXIOS_TIMEOUT_MS, DEFAULT_LIMIT, MAX_LIMIT, CACHE_DB_PATH, CACHE_EVICTION_DAYS } = require('./config');`

    Im try-Block des DB-Init (nach dem `db.exec(DELETE...)` für leere Einträge, VOR den `db.prepare()`-Zeilen):
    Füge ein:
    ```javascript
    // PERF-01: Eviction — remove entries older than CACHE_EVICTION_DAYS days
    const evictionThreshold = Date.now() - CACHE_EVICTION_DAYS * 24 * 60 * 60 * 1000;
    const evicted = db.prepare('DELETE FROM search_cache WHERE created_at < ?').run(evictionThreshold);
    if (evicted.changes > 0) {
        db.exec('VACUUM');
    }
    logger.info({ evictedRows: evicted.changes, thresholdDays: CACHE_EVICTION_DAYS }, 'cache eviction complete');
    ```

    TEIL B — Request-Deduplication:

    Direkt nach der Klassendefinition `class StorytelProvider {` (vor dem constructor) füge eine Klasseneigenschaft hinzu:
    Diese muss als Modul-Level-Variable definiert werden, NICHT als Klasseneigenschaft, weil alle Provider-Instanzen dieselbe Map teilen sollen:

    Füge NACH dem `let setCache = null;` (Zeile 13) und VOR der `class StorytelProvider {` Definition ein:
    ```javascript
    // PERF-03: In-flight deduplication — prevents duplicate API calls for concurrent identical queries
    const inFlight = new Map();
    ```

    In der `searchBooks()`-Methode: NACH dem Cache-Check-Block (nach dem `if (getCache) { ... }` Block, Zeilen 443-451) und VOR dem `try {` für den API-Call, füge ein:

    ```javascript
    // PERF-03: Return existing promise if same query is already in-flight
    if (inFlight.has(cacheKey)) {
        logger.debug({ key: cacheKey }, 'deduplication hit — reusing in-flight request');
        return inFlight.get(cacheKey);
    }
    ```

    Dann wrape den gesamten `try { ... } catch { ... }` Block in eine Promise und registriere sie in der Map:

    Ersetze:
    ```javascript
    try {
        let searchData = await this.fetchFromApi(formattedQuery, locale);
        // ... (kompletter try-catch Block bis zum abschließenden })
    } catch (error) {
        logger.error({ err: error.message, query: formattedQuery }, 'Storytel API request failed');
        return { matches: [] };
    }
    ```

    durch:
    ```javascript
    const apiCallPromise = (async () => {
        try {
            let searchData = await this.fetchFromApi(formattedQuery, locale);
            let books = searchData?.books || [];
            logger.info({ count: books.length, query: formattedQuery }, 'search results received');

            // Retry without author if combined search found nothing
            if (books.length === 0 && author) {
                const queryOnly = cleanQuery.replace(/\s+/g, '+');
                logger.info({ query: queryOnly }, 'retrying without author');
                searchData = await this.fetchFromApi(queryOnly, locale);
                books = searchData?.books || [];
                logger.info({ count: books.length }, 'retry results received');
            }

            let matches = [];
            for (const book of books) {
                if (!book.book || !book.book.id) continue;
                const metadata = this.formatBookMetadata({ slb: book }, type);
                if (metadata) {
                    matches.push(metadata);
                }
            }

            // Sort by author relevance if author was provided, then limit
            if (author) {
                matches.sort((a, b) =>
                    this.authorMatchScore(b.author, author) - this.authorMatchScore(a.author, author)
                );
            }
            matches = matches.slice(0, maxResults);

            const result = { matches };

            // Only cache non-empty results
            if (matches.length > 0 && setCache) {
                try {
                    setCache.run(cacheKey, JSON.stringify(result), Date.now());
                    logger.debug({ key: cacheKey }, 'cache write');
                } catch (err) {
                    logger.warn({ err: err.message, key: cacheKey }, 'cache write failed');
                }
            }

            return result;
        } catch (error) {
            logger.error({ err: error.message, query: formattedQuery }, 'Storytel API request failed');
            return { matches: [] };
        } finally {
            inFlight.delete(cacheKey);
        }
    })();

    inFlight.set(cacheKey, apiCallPromise);
    return apiCallPromise;
    ```

    WICHTIG: Der finally-Block sorgt dafür, dass der cacheKey aus der Map entfernt wird — egal ob Erfolg oder Fehler.
  </action>
  <verify>
    node -e "
    const { StorytelProvider } = require('./src/provider');
    const fs = require('fs');
    const src = fs.readFileSync('./src/provider.js', 'utf8');
    console.assert(src.includes('inFlight'), 'FAIL: inFlight not found');
    console.assert(src.includes('CACHE_EVICTION_DAYS'), 'FAIL: CACHE_EVICTION_DAYS not found');
    console.assert(src.includes('evictionThreshold'), 'FAIL: evictionThreshold not found');
    console.assert(src.includes('finally'), 'FAIL: finally block not found');
    console.log('PASS: all patterns found');
    "
  </verify>
  <acceptance_criteria>
    - `grep "inFlight" src/provider.js` gibt mindestens 3 Treffer (Deklaration, .has(), .set(), .delete())
    - `grep "evictionThreshold" src/provider.js` gibt einen Treffer zurück
    - `grep "CACHE_EVICTION_DAYS" src/provider.js` gibt mindestens 2 Treffer (import + verwendung)
    - `grep "finally" src/provider.js` gibt einen Treffer zurück
    - `node -e "require('./src/provider')"` läuft ohne Fehler (Syntax ok)
  </acceptance_criteria>
  <done>
    - Cache-Eviction läuft beim Start: Einträge älter als 30 Tage werden gelöscht, bei Änderungen VACUUM ausgeführt, Log-Eintrag mit evictedRows erscheint
    - In-flight Map verhindert doppelte API-Calls: zweite identische Anfrage während erster läuft gibt Promise des ersten zurück
    - finally-Block garantiert Aufräumen der inFlight Map nach Abschluss
  </done>
</task>

</tasks>

<verification>
# Syntax-Check
node -e "require('./src/provider')" && echo "PASS: module loads"

# Konstante vorhanden
node -e "const c = require('./src/config'); console.log('CACHE_EVICTION_DAYS:', c.CACHE_EVICTION_DAYS);"

# Pattern-Check
grep -c "inFlight" src/provider.js
grep -n "evictionThreshold\|CACHE_EVICTION_DAYS\|finally" src/provider.js
</verification>

<success_criteria>
- node require('./src/provider') läuft ohne Fehler
- inFlight Map ist deklariert und in searchBooks() verwendet
- Cache-Eviction-Code ist im DB-Init-Block vorhanden
- CACHE_EVICTION_DAYS ist in config.js und provider.js vorhanden
- finally-Block löscht cacheKey aus inFlight Map nach Abschluss
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-performance/03-02-SUMMARY.md` erstellen.
</output>
